const initialState = {
  name:"dilserg",
  photo:null,
  totalIncome:150
}

const profileReducer = (state= initialState, action) =>{
  switch (action.type){
    default:
      return state
  }
}

export default profileReducer